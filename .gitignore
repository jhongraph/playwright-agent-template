node_modules/
test-results/
playwright-report/
.playwright/
dist/
*.js.map

data/reports.json
test-results/

# Env con credenciales — nunca comitear
.env.playwright

# Estado runtime de agentes — no versionar
.agent-state/*.json
!.agent-state/*.schema.json
!.agent-state/.gitkeep
